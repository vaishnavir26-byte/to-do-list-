# to-do-list-
To-Do List App is a simple command-line application built with Python that helps users manage their daily tasks. It allows users to add new tasks, view all tasks, mark tasks as completed, and delete tasks. The application stores tasks in a JSON file, ensuring that data is saved even after the program is closed. 
